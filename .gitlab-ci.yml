pages:
  stage: deploy
  script:
    - rm -rf public
    - mkdir public
    - cp index.html public/
    - cp -r media public/
    - cp -r videos public/
  artifacts:
    paths:
      - public
  only:
    - master
